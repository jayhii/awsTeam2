# 도메인 데이터 관리 파이프라인 가이드

## 개요

도메인 데이터를 지속적으로 관리하고 업데이트하는 자동화 파이프라인입니다.

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    데이터 소스                                │
├─────────────────────────────────────────────────────────────┤
│  • GitHub API (기술 트렌드)                                   │
│  • Stack Overflow (기술 인기도)                               │
│  • 신규 프로젝트 등록                                          │
│  • 신규 직원 추가                                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                Lambda 함수 (자동 처리)                         │
├─────────────────────────────────────────────────────────────┤
│  1. TechTrendCollector                                       │
│     - 주기적 실행 (EventBridge: 매주)                         │
│     - GitHub API 호출                                        │
│     - Claude AI 트렌드 분석                                   │
│     - TechTrends 테이블 업데이트                              │
│                                                              │
│  2. DomainPortfolioUpdater                                   │
│     - 실시간 실행 (DynamoDB Stream)                           │
│     - Projects 테이블 변경 감지                               │
│     - Employees 테이블 변경 감지                              │
│     - 도메인 통계 자동 업데이트                                │
│                                                              │
│  3. DomainAnalysisEngine                                     │
│     - API 요청 시 실행                                        │
│     - 실시간 도메인 분석                                       │
│     - 신규 도메인 추천                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  DynamoDB 테이블                              │
├─────────────────────────────────────────────────────────────┤
│  • TechTrends (기술 트렌드)                                   │
│  • Projects (프로젝트 + 도메인 정보)                           │
│  • Employees (직원 + 도메인 경험)                             │
│  • DomainPortfolio (도메인 포트폴리오)                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    프론트엔드 UI                              │
├─────────────────────────────────────────────────────────────┤
│  • 도메인 분석 대시보드                                        │
│  • 실시간 트렌드 차트                                          │
│  • 도메인 포트폴리오 관리                                       │
│  • 신규 도메인 추천                                            │
└─────────────────────────────────────────────────────────────┘
```

## 1. 기술 트렌드 수집 (TechTrendCollector)

### 기능
- **GitHub API 연동**: 주요 기술 리포지토리의 스타, 포크, 이슈 수 수집
- **Claude AI 분석**: Stack Overflow 트렌드 분석
- **자동 업데이트**: TechTrends 테이블 주기적 업데이트
- **신규 기술 발견**: 떠오르는 기술 자동 추가

### 실행 주기
- **EventBridge Rule**: 매주 일요일 오전 2시 (UTC)
- **수동 실행**: Lambda 콘솔에서 테스트 가능

### 수집 데이터
```json
{
  "tech_name": "React",
  "category": "Frontend",
  "trend_score": 92,
  "demand_score": 93,
  "growth_rate": 10.2,
  "market_share": 42.8,
  "github_stars": 220000,
  "related_domains": ["E-commerce", "Healthcare"],
  "last_updated": "2024-12-02T10:00:00Z"
}
```

### 배포 방법
```bash
# Lambda 함수 생성
cd lambda_functions/tech_trend_collector
zip -r function.zip .
aws lambda create-function \
  --function-name TechTrendCollector \
  --runtime python3.11 \
  --handler index.handler \
  --role arn:aws:iam::ACCOUNT_ID:role/LambdaExecutionRole \
  --zip-file fileb://function.zip \
  --timeout 300 \
  --memory-size 512

# EventBridge Rule 생성
aws events put-rule \
  --name TechTrendCollectionSchedule \
  --schedule-expression "cron(0 2 ? * SUN *)"

aws events put-targets \
  --rule TechTrendCollectionSchedule \
  --targets "Id"="1","Arn"="arn:aws:lambda:REGION:ACCOUNT_ID:function:TechTrendCollector"
```

## 2. 도메인 포트폴리오 업데이트 (DomainPortfolioUpdater)

### 기능
- **실시간 감지**: DynamoDB Stream을 통한 변경 감지
- **자동 분류**: 신규 프로젝트 도메인 자동 분류
- **통계 업데이트**: 도메인별 프로젝트/인력 수 자동 계산
- **연관성 분석**: 지식 도메인 ↔ 기술 도메인 매핑

### 트리거
- **Projects 테이블**: INSERT, MODIFY 이벤트
- **Employees 테이블**: INSERT, MODIFY 이벤트

### 처리 로직

#### 신규 프로젝트 추가 시
1. 프로젝트 이름/설명에서 도메인 자동 분류
2. 기술 스택 분석하여 기술 도메인 매핑
3. 해당 도메인 프로젝트 수 증가
4. 도메인 간 연관성 업데이트

#### 신규 직원 추가 시
1. 보유 기술 분석
2. 진입 가능한 도메인 식별
3. 도메인별 가용 인력 수 증가
4. 도메인 전문가 목록 업데이트

### 배포 방법
```bash
# Lambda 함수 생성
cd lambda_functions/domain_portfolio_updater
zip -r function.zip .
aws lambda create-function \
  --function-name DomainPortfolioUpdater \
  --runtime python3.11 \
  --handler index.handler \
  --role arn:aws:iam::ACCOUNT_ID:role/LambdaExecutionRole \
  --zip-file fileb://function.zip \
  --timeout 60 \
  --memory-size 256

# DynamoDB Stream 연결
aws lambda create-event-source-mapping \
  --function-name DomainPortfolioUpdater \
  --event-source-arn arn:aws:dynamodb:REGION:ACCOUNT_ID:table/Projects/stream/STREAM_ID \
  --starting-position LATEST

aws lambda create-event-source-mapping \
  --function-name DomainPortfolioUpdater \
  --event-source-arn arn:aws:dynamodb:REGION:ACCOUNT_ID:table/Employees/stream/STREAM_ID \
  --starting-position LATEST
```

## 3. 도메인 분석 엔진 (DomainAnalysisEngine)

### 기능
- **실시간 분석**: 최신 데이터 기반 도메인 분석
- **AI 추천**: Claude를 활용한 진입 전략 제시
- **실현 가능성 계산**: 기술 보유율, 인력 가용성 기반 점수화
- **팀 추천**: 도메인 진입을 위한 최적 팀 구성 제안

### API 엔드포인트
```
POST /domain-analysis
{
  "analysis_type": "new_domains" | "expansion_strategy"
}
```

### 응답 예시
```json
{
  "current_domains": ["Finance", "Healthcare", "E-commerce"],
  "identified_domains": [
    {
      "domain_name": "Aviation",
      "feasibility_score": 75.5,
      "required_skills": ["React Native", "Flutter", "Real-time"],
      "matched_skills": ["React", "Node.js", "AWS"],
      "skill_gap": ["Flutter", "Real-time"],
      "transferable_employees": 12,
      "recommended_team": ["U_001", "U_003", "U_015"],
      "reasoning": "모바일 개발 역량이 강하며..."
    }
  ]
}
```

## 4. 프론트엔드 UI 개선

### 새로운 기능

#### 4.1 도메인 포트폴리오 대시보드
```typescript
// 현재 보유 도메인 현황
- 도메인별 프로젝트 수
- 도메인별 전문가 수
- 도메인 성숙도 (Beginner/Intermediate/Advanced)
- 최근 업데이트 시간
```

#### 4.2 실시간 기술 트렌드 차트
```typescript
// TechTrends 데이터 시각화
- 트렌드 점수 TOP 10
- 성장률 TOP 10
- 도메인별 주요 기술
- 시간별 트렌드 변화
```

#### 4.3 신규 도메인 추천
```typescript
// AI 기반 추천
- 실현 가능성 점수
- 필요 기술 vs 보유 기술
- 추천 팀 구성
- 진입 전략 (채용/교육/파트너십)
```

#### 4.4 도메인 관리 기능
```typescript
// 관리자 기능
- 도메인 추가/수정/삭제
- 도메인 목표 설정
- 진행 상황 추적
- 알림 설정
```

## 5. 데이터 흐름

### 신규 프로젝트 등록 시
```
1. 사용자가 프로젝트 등록
   ↓
2. Projects 테이블에 INSERT
   ↓
3. DynamoDB Stream 트리거
   ↓
4. DomainPortfolioUpdater 실행
   ↓
5. 도메인 자동 분류 및 통계 업데이트
   ↓
6. 프론트엔드 대시보드 자동 갱신
```

### 기술 트렌드 업데이트 시
```
1. EventBridge 스케줄 트리거 (매주)
   ↓
2. TechTrendCollector 실행
   ↓
3. GitHub API 호출 + Claude 분석
   ↓
4. TechTrends 테이블 업데이트
   ↓
5. 프론트엔드 트렌드 차트 갱신
```

## 6. 모니터링 및 알림

### CloudWatch 메트릭
- Lambda 실행 횟수
- 오류 발생 횟수
- 처리 시간
- DynamoDB 읽기/쓰기 용량

### 알림 설정
```bash
# SNS 토픽 생성
aws sns create-topic --name DomainPipelineAlerts

# CloudWatch 알람 생성
aws cloudwatch put-metric-alarm \
  --alarm-name TechTrendCollectorErrors \
  --alarm-description "기술 트렌드 수집 오류" \
  --metric-name Errors \
  --namespace AWS/Lambda \
  --statistic Sum \
  --period 300 \
  --threshold 1 \
  --comparison-operator GreaterThanThreshold
```

## 7. 비용 최적화

### Lambda 함수
- **TechTrendCollector**: 주 1회 실행, 5분 이내 완료
- **DomainPortfolioUpdater**: 이벤트 기반, 초당 1-2회
- **예상 비용**: 월 $5 이하

### DynamoDB
- **On-Demand 모드**: 실제 사용량만 과금
- **예상 비용**: 월 $10-20

### API Gateway
- **REST API**: 요청당 과금
- **예상 비용**: 월 $5 이하

**총 예상 비용**: 월 $20-30

## 8. 테스트

### 수동 테스트
```bash
# 기술 트렌드 수집 테스트
aws lambda invoke \
  --function-name TechTrendCollector \
  --payload '{}' \
  response.json

# 도메인 분석 API 테스트
curl -X POST https://API_ID.execute-api.REGION.amazonaws.com/prod/domain-analysis \
  -H "Content-Type: application/json" \
  -d '{"analysis_type": "new_domains"}'
```

### 자동 테스트
```python
# deployment/test_domain_pipeline.py
python deployment/test_domain_pipeline.py
```

## 9. 유지보수

### 주기적 작업
- **월 1회**: 기술 트렌드 데이터 검증
- **분기 1회**: 도메인 분류 규칙 업데이트
- **반기 1회**: AI 모델 재학습

### 로그 확인
```bash
# Lambda 로그 확인
aws logs tail /aws/lambda/TechTrendCollector --follow

# DynamoDB Stream 로그
aws logs tail /aws/lambda/DomainPortfolioUpdater --follow
```

## 10. 확장 계획

### Phase 1 (현재)
- ✅ 기본 도메인 분류
- ✅ 기술 트렌드 수집
- ✅ 자동 통계 업데이트

### Phase 2 (다음 분기)
- 🔄 외부 API 추가 (LinkedIn, Indeed)
- 🔄 머신러닝 기반 도메인 예측
- 🔄 실시간 알림 시스템

### Phase 3 (향후)
- 📋 도메인 로드맵 자동 생성
- 📋 경쟁사 분석
- 📋 시장 기회 발견

## 참고 자료

- [AWS Lambda 문서](https://docs.aws.amazon.com/lambda/)
- [DynamoDB Streams](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Streams.html)
- [EventBridge 스케줄](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-create-rule-schedule.html)
- [GitHub API](https://docs.github.com/en/rest)
